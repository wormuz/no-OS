# IIOD is served over TCP on the linux platform, so the BSD socket backend is
# required. CONFIG_LINUX_SOCKET already defaults to y when IIO is enabled; it is
# stated explicitly here because the transport is the point of this variant.
CONFIG_LINUX_SOCKET=y
